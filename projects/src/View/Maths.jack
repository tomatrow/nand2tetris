class Maths {
    function Float arctan(Float x, Float y) {

    }
    function Float sin(Float x) {

    }
    function Float cos(Float x) {

    }
    function int pow(int x, int n) {
        var int result;
        if (n == 0) {
            return 1;
        }

        let result = x;

        while (n > 0) {
            do n = n - 1;
            do result = Math.multiply(result,x);
        }

        return result;
    }
}
